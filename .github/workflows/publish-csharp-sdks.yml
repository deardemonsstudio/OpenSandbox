name: OpenSandbox Workflow — SYSTEM FREEZE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  system-freeze:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse execution during pristine audit gate
        run: |
          echo 'SYSTEM FREEZE — NOT READY TO RESTART.'
          echo 'This historical OpenSandbox workflow ref is suspended until the pristine restart gate is completed and Kirstie explicitly authorises restart.'
          exit 1

# Historical Dependabot ref retained for provenance. Deploy, nightly, publish and release execution are suspended.
